[phases.install]
cmds = ["uv sync --locked --no-dev --extra http --extra mcp"]

[phases.build]
cmds = ["uv sync --locked --no-dev --no-editable --extra http --extra mcp"]

[start]
cmd = "cd /app && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
