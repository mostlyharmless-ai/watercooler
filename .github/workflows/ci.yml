name: CI

on:
  push:
    branches: [ main, staging ]
  pull_request:
    branches: [ main, staging ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install package with all extras
      # Note: Using graphiti instead of memory to avoid private LeanRAG dependency
      # LeanRAG tests are skipped anyway (integration_leanrag_llm marker)
      run: pip install -e ".[dev,graphiti]"

    - name: Test import
      run: python -c "import watercooler; print(f'watercooler {watercooler.__version__}')"

    - name: Run tests
      run: pytest tests/ -v -m "not integration_falkor and not integration_leanrag_llm and not integration_graphiti"
